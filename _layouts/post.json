---
---
{
  "title"        : {{ page.title | jsonify }},
  "categories"   : {{ page.categories | jsonify }},
  "date"         : {{ page.date | jsonify }},
  "url"          : {{ page.url | jsonify }},
  "content"      : {{ content | jsonify }},
  "next_url"     : {{ page.next.url | jsonify }},
  "previous_url" : {{ page.previous.url | jsonify }}
}

